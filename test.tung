var amount = input("How many numbers do you want to add? ")
var total = 0
var current = 1

while current <= int(amount) {
    var number = input("Please enter a number " + str(current) + " - ")
    total = total + number
    current = current + 1
}

print(total)